{
  "name": "pact",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "setup": "npm install --prefix api && npm install --prefix web"
  }
}
